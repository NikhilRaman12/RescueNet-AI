# Architecture

RescueNet Slack keeps the existing RescueNet AI backend and adds a Slack-native interface around it. The backend remains Python-first and uses the existing rescue graph for disaster reasoning.

See the root `ARCHITECTURE.md` for the full flow diagram and component map.
